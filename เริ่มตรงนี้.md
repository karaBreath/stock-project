# เริ่มตรงนี้ — ใช้งานบนคอมของตัวเอง

> ไฟล์นี้สำหรับคนที่นั่งอยู่หน้าคอมพิวเตอร์ Windows แล้วอยากเปิดแอปให้ได้เร็วที่สุด

---

## ครั้งแรกสุด (ยังไม่เคยมีโค้ดในเครื่อง)

ต้องมี 2 อย่างก่อน — ถ้ามีแล้วข้ามไปได้เลย

| ต้องมี | โหลดจาก | ข้อควรระวัง |
|---|---|---|
| **Python 3.10 ขึ้นไป** | https://www.python.org/downloads/ | ตอนติดตั้ง **ต้องติ๊ก `Add python.exe to PATH`** ไม่งั้นเปิดไม่ได้ |
| **Git** | https://git-scm.com/download/win | กด Next รัวได้ ไม่ต้องตั้งค่าอะไร |

จากนั้นเปิด **Command Prompt** (กดปุ่ม Windows → พิมพ์ `cmd` → Enter) แล้วพิมพ์ทีละบรรทัด:

```bat
cd %USERPROFILE%\Desktop
git clone https://github.com/karaBreath/stock-project.git
cd stock-project
git checkout claude/stock-trading-project-zjrrcz
gui.bat
```

> ⚠️ `cd %USERPROFILE%\Desktop` คือคำสั่งจริง ๆ พิมพ์ตามนี้ได้เลย
> (อย่าพิมพ์ `C:\path\to\...` ตามตัวอย่างที่เห็นในเว็บทั่วไป — นั่นเป็นแค่ตัวแทนชื่อโฟลเดอร์)

`gui.bat` จะเปิด **หน้าต่างควบคุม "เทรดข่าวโลก"** ขึ้นมา — จากนั้นไม่ต้องพิมพ์คำสั่งอีกเลย
กดปุ่มเอาทั้งหมด ตัวโปรแกรมจะสร้าง virtual environment → ติดตั้งไลบรารี →
สร้างไฟล์ `.env` → เปิดเบราว์เซอร์ให้เอง **ครั้งแรกใช้เวลา 2-5 นาที**

---

## หน้าต่างควบคุม "เทรดข่าวโลก" — ปุ่มแต่ละอันทำอะไร

| ปุ่ม | ทำอะไร |
|---|---|
| ▶ **เปิดโปรแกรม** | เปิดเว็บบนเครื่องนี้ พอพร้อมจะเด้งเบราว์เซอร์ให้เอง |
| 🌐 **เปิดหน้าเว็บ** | เปิดแท็บใหม่ไปที่เว็บที่รันอยู่ |
| ■ **ปิดโปรแกรม** | ปิดทุกอย่างที่หน้าต่างนี้เปิดไว้ |
| 📱 **เปิดให้ดูจากมือถือ** | เปิดออกอินเทอร์เน็ตผ่าน Cloudflare · ลิงก์จะขึ้นให้กด **คัดลอกลิงก์** |
| 🔗 **ตั้งโดเมนตัวเอง** | ตั้งลิงก์คงที่ (`nebula.twinpatta.com`) ทำครั้งเดียวพอ |
| ⬇ **อัปเดตโปรแกรม** | ดึงโค้ดใหม่ล่าสุดจาก GitHub |
| 🩺 **ตรวจระบบ** | บอกเป็นข้อ ๆ ว่าอะไรขาด เวลาเปิดไม่ขึ้น |
| 🖥 **สร้างไอคอนบนหน้าจอ** | สร้างไอคอนชื่อ **เทรดข่าวโลก** ไว้หน้าจอ |

มุมขวาบนบอกสถานะตลอดเวลา — **● กำลังทำงาน** หรือ **○ ยังไม่ได้เปิด**
ช่องล่างคือบันทึกการทำงาน เวลามีปัญหาให้ดูบรรทัดสุดท้ายในนั้น

---

## ทำไอคอนไว้หน้าจอ (แนะนำ — ทำครั้งเดียว)

ในหน้าต่างควบคุม กด **🖥 สร้างไอคอนบนหน้าจอ**

จะได้ไอคอนชื่อ **เทรดข่าวโลก** อยู่บนหน้าจอ ครั้งต่อไป **ดับเบิลคลิกไอคอนนั้นอันเดียวจบ**
ไม่ต้องเข้าโฟลเดอร์ ไม่ต้องพิมพ์อะไร และ**ไม่มีหน้าต่างดำเด้งขึ้นมา**

> รองรับเครื่องที่เปิด OneDrive ด้วย — ระบบถาม Windows ว่าหน้าจอจริง ๆ อยู่ที่ไหน
> ไม่ได้เดาว่าเป็น `%USERPROFILE%\Desktop` เสมอ

---

## ครั้งต่อ ๆ ไป

**ดับเบิลคลิกไอคอน "เทรดข่าวโลก" บนหน้าจอ** แล้วกด ▶ เปิดโปรแกรม — จบ

ถ้ายังไม่ได้สร้างไอคอน จะเข้าโฟลเดอร์ `stock-project` แล้วดับเบิลคลิกไฟล์ตรง ๆ ก็ได้:

| ไฟล์ | ใช้เมื่อ |
|---|---|
| `gui.bat` | **หน้าต่างควบคุมรวมทุกอย่าง** (แนะนำ) |
| `start.bat` | เปิดแอปเฉย ๆ แบบหน้าต่างดำ |
| `update.bat` | ดึงโค้ดใหม่ล่าสุดมาก่อน แล้วเปิดแอป |
| `share.bat` | เปิดแอป **พร้อมเปิดออกอินเทอร์เน็ต** ให้ดูจากมือถือได้ |
| `setup-domain.bat` | ตั้งลิงก์คงที่เป็นโดเมนตัวเอง (ทำครั้งเดียวพอ) |

เว็บจะเปิดที่ **http://127.0.0.1:5000** และเปิดเบราว์เซอร์ให้เอง
ปิดแอปด้วยปุ่ม **■ ปิดโปรแกรม** ในหน้าต่างควบคุม
(ถ้าเปิดแบบหน้าต่างดำ ให้กด **Ctrl + C** หรือปิดหน้าต่างนั้นไปเลย)

---

## ถ้าเปิดไม่ได้ ทำตามนี้

**1) ตรวจระบบก่อน** — ในหน้าต่างควบคุม กด **🩺 ตรวจระบบ**
จะบอกเป็นข้อ ๆ ว่าอะไรขาด โดยไม่เปิดเว็บ (เทียบเท่าคำสั่ง `start.bat --check`)

**2) อาการที่เจอบ่อย**

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| ดับเบิลคลิกแล้วไม่มีอะไรขึ้นเลย | Python ไม่ได้ติ๊ก Add to PATH ตอนติดตั้ง — ลงใหม่แล้วติ๊ก |
| หน้าต่างดำเด้งขึ้นแล้วหายทันที | สาเหตุเดียวกัน — Python ไม่อยู่ใน PATH |
| กด "สร้างไอคอนบนหน้าจอ" แล้วบอก Access denied | หน้าจอถูกล็อกสิทธิ์ — คลิกขวา `gui.bat` → Run as administrator ครั้งเดียว |
| `git not found` | ยังไม่ได้ลง Git |
| `git pull failed` บอกว่ามีไฟล์แก้ค้าง | พิมพ์ `git stash` แล้วรัน `update.bat` ใหม่ |
| เปิดได้แต่ราคาหุ้นขึ้น `—` | เน็ตเข้า Yahoo Finance ไม่ได้ชั่วคราว รอสักครู่แล้วรีเฟรช |
| ลูกโลกยังไม่มีจุด | ปกติ — ระบบทยอยเก็บข่าวเบื้องหลัง ขึ้นเองใน 1-2 นาที ไม่ต้องกดอะไร |

---

## เปิดเว็บให้ดูจากมือถือ/ที่อื่นได้ (Cloudflare Tunnel)

ในหน้าต่างควบคุม กด **📱 เปิดให้ดูจากมือถือ** (หรือดับเบิลคลิก `share.bat`)
จะเปิดเว็บออกอินเทอร์เน็ตจากเครื่องนี้เลย
ไม่ต้องเช่าเซิร์ฟเวอร์ ไม่มีค่าใช้จ่าย ไม่มีอาการหลับแล้วรอปลุก
ข้อมูลทุกอย่างยังอยู่บนเครื่องคุณ Cloudflare แค่ส่งต่อคำขอเข้ามา

ครั้งแรกจะโหลด `cloudflared` ให้เอง (~35 MB ครั้งเดียว) แล้วขึ้นลิงก์แบบนี้:

```
ลิงก์สำหรับเปิดจากมือถือ/ที่อื่น:
https://xxxx-yyyy-zzzz.trycloudflare.com/?k=<กุญแจ>
```

ลิงก์จะขึ้นในหน้าต่างควบคุมพร้อมปุ่ม **คัดลอกลิงก์** — กดแล้วส่งเข้ามือถือได้เลย
เปิดลิงก์นั้นในมือถือครั้งเดียว เครื่องนั้นจะจำไว้ 30 วัน ไม่ต้องใส่กุญแจซ้ำ

### 🔒 เรื่องความปลอดภัยที่ต้องรู้

เปิด tunnel = **ใครก็ตามที่ได้ลิงก์ไป จะเห็นพอร์ตหุ้น ไม้ที่ถืออยู่ใน MT5
และยอดเงินในบัญชีของคุณ** ระบบจึงสุ่ม "กุญแจ" ให้อัตโนมัติและบังคับใช้เสมอ

| สถานการณ์ | ผลลัพธ์ |
|---|---|
| เปิดจากเครื่องตัวเอง (`127.0.0.1`) | เข้าได้เลย ไม่ต้องใส่กุญแจ |
| คนนอกที่ไม่มีกุญแจ | เห็นแค่หน้า "ต้องใช้กุญแจ" ไม่เห็นข้อมูลใด ๆ |
| คนนอกที่มีกุญแจ | เข้าได้ครบเหมือนคุณ |

- **ห้ามส่งลิงก์นี้ให้ใคร** เท่ากับให้สิทธิ์ดูพอร์ตทั้งหมด
- กุญแจเก็บใน `.env` ซึ่งไม่ถูก commit ขึ้น git
- ปิด tunnel = ปิดหน้าต่างสีดำ หรือกด **Ctrl + C** เว็บจะไม่เปิดออกเน็ตทันที
### อยากได้ลิงก์คงที่ (โดเมนตัวเอง)

ลิงก์สุ่มเปลี่ยนทุกครั้งที่เปิด ต้องส่งลิงก์ใหม่เข้ามือถือตลอด
ตั้งโดเมนตัวเองครั้งเดียวแล้วจบ:

ในหน้าต่างควบคุม กด **🔗 ตั้งโดเมนตัวเอง** (หรือดับเบิลคลิก `setup-domain.bat`)

จะพาทำทีละขั้น (เข้าสู่ระบบ Cloudflare → สร้าง tunnel → ชี้ DNS ให้อัตโนมัติ)
เสร็จแล้วครั้งต่อไป **ดับเบิลคลิก `share.bat` เฉย ๆ** ก็ได้ลิงก์เดิมตลอด

| ต้องมีอะไรบ้าง | หมายเหตุ |
|---|---|
| บัญชี Cloudflare | **ฟรี ไม่ต้องเสียเงิน** |
| โดเมนหลักอยู่ใน Cloudflare | ต้องเพิ่มโดเมนเข้า Cloudflare และย้าย nameserver แล้ว |

ถ้าโดเมนหลักย้ายเข้า Cloudflare ไม่ได้ (เช่นผู้ให้บริการอื่นจัดการอยู่)
จะใช้ลิงก์สุ่มต่อไปก็ได้ ทำงานเหมือนกันทุกอย่าง ต่างแค่ลิงก์เปลี่ยน

## เชื่อมกับระบบเทรด MT5 (ถ้าใช้)

`start.bat` ตั้งค่าให้แล้วโดยอัตโนมัติที่ `VE_BASE_URL=http://127.0.0.1:8641`
แค่เปิดระบบ volume-edge ไว้บนเครื่องเดียวกัน แล้วเข้าเมนู **พอร์ต MT5**

การเชื่อมนี้ **อ่านอย่างเดียว** — ดูสถานะ/ไม้ที่ถืออยู่/สถิติได้ แต่สั่งซื้อขายไม่ได้

---

## หมายเหตุเรื่องความปลอดภัย

- ไฟล์ `.env` เก็บค่าตั้งค่าส่วนตัว **ไม่ถูก commit ขึ้น git** (อยู่ใน `.gitignore` แล้ว)
- แอปรันอยู่บนเครื่องคุณเท่านั้น ไม่มีการส่งข้อมูลพอร์ตออกไปไหน
- ข้อมูลทั้งหมดเพื่อการศึกษา **ไม่ใช่คำแนะนำการลงทุน**
